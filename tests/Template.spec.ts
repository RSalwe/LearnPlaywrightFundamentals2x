//This file is to import the below code directly in any new file instead of write it manually

import { test, expect } from '@playwright/test'

test('Verify our first TC', async ({ page }) => {




});